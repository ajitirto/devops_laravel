name: First pipeline

on:
    push:
        branches: ["master"]

jobs:
    test: 
        runs-on: ubuntu-latest
        steps: 
            - uses: actions/checkout@v3 
            - name: Test
                run: cp README.md example.test 


                
